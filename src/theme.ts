const colors = {
  gray: "#e8ebef",
  brown: "#8c6d45",
};

const mediaSize = {
  xxl: "screen and (max-width: 1450px)",
};

const theme = {
  colors,
  mediaSize,
};

export default theme;
export type Theme = typeof theme;
